# Legacy workspaces

These directories are **deprecated** relative to the trigger/train/validation lab.

They contain older scaffolding evals (`nextjs-*-test`, `product-manager-test`) and the
hand-iterated trigger experiments under `feature-expert-tests/feature-expert-triggert/`.

## Current lab location

Use instead:

- Tooling: [`lab/`](../../lab/)
- Per-skill experiments: [`workspace/skills/<skill-name>/`](../skills/)
- Canonical skills: [`development/skills/`](../../development/skills/)

Do not add new trigger evaluations here. Migrate new skills into `workspace/skills/`.
